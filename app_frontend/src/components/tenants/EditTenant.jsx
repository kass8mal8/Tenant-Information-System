import { Box, Modal } from "@mui/material"

const EditTenant = ({open}) => {
    return (  
        <Box>
            <Modal open={open}>Hello world</Modal>
        </Box>
    );
}
 
export default EditTenant;