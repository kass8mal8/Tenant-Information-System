const num = "5436977225233"
const modfied = `0${num.slice(3, )}`
// console.log( modfied.length)

if((modfied.length !== 10) || (modfied.length !== 9)) {
    console.log(modfied)
}
else {
    console.log("Wrong")
}